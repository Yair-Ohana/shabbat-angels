import React from "react";

import { Link } from "react-router-dom";

const Header = () => {
  return (
    <div className="flex items-center justify-center h-14 bg-third border-b border-mainText"></div>
  );
};

export default Header;
